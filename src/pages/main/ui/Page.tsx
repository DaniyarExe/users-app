import style from "./Page.module.scss";
import { UserCard } from "@/entities/ui/Card/UserCard";

const Page = () => {
  
  return (
    <section>
      <UserCard />
    </section>
  );
};

export default Page;
